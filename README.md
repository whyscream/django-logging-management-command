# Django logging management command

A proof of concept / reference implementation of a Django management command base class that supports logging and verbosity handling by default.

For details, see https://forum.djangoproject.com/t/implement-default-handling-of-verbosity-in-management-commands/44892

# TODO

A list of open todo items according to the ideas in the forum post:

- [x] Implement a command-level logger and apply the `--verbosity` flag on it.
- [ ] Test (and possibly implement) that `settings.LOGGING` is adhered.
- [ ] Enforce logging output to `stdout`and/or `stderr`.
- [ ] Allow to apply verbosity to the containing Django app.
- [ ] Allow to apply verbosity to logging generated by imported libraries.
- [ ] Write documentation prose for Django docs.
- [ ] Implement a simple mechanism to control verbosity of `Command.stdout.write()`. Maybe `.write("msg", ending=..., at_verbosity=None)`?

# Setup

`uv sync` should setup a working environment for developing or testing.

# Testing

Use `pytest` to run the included tests.

# License

All code is `BSD 3-clause` licensed, See [LICENSE](LICENSE) for details.
